export interface IGetFilePathsFromDirSyncOptions {
  excludeText?: string;
  throwError?: boolean;
  errorMessage?: string;
}

export interface IReadDirOptions {
  throwError?: boolean;
  errorMessage?: string;
}
